repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.4.4
    hooks:
      - id: ruff          # linter (+ auto-fix)
        args: [--fix]
      - id: ruff-format   # formatter (équivalent black)
